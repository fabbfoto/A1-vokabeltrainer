{
  "name": "vokabeltrainer",
  "version": "1.0.0",
  "dependencies": {
    "@google-cloud/text-to-speech": "^5.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "scripts": {
    "build": "mkdir -p dist && cp packages/trainer-basis/index.html packages/trainer-basis/trainer.js packages/trainer-basis/vokabular.js packages/trainer-themen/manifest.json dist/ && cp -r shared dist/",
    "serve": "http-server dist -c-1"
  }
}
